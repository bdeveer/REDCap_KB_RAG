{
  "eval_id": 1,
  "eval_name": "wrong-operators",
  "prompt": "Can you check this branching logic? It's not showing the field when I expect it to:\n\n[age] >= 18 && [consent_status] == 'yes' && [site] != 'withdrawn'",
  "assertions": [
    "Identifies && as a syntax error and replaces with AND",
    "Identifies == as a syntax error and replaces with =",
    "Identifies != as a syntax error and replaces with <>",
    "Classifies all three as syntax errors, not reference errors",
    "Provides a complete corrected expression"
  ]
}
